export const xAxisPositions = new Float32Array([
  // 1
  -1, 0, 0,
  // 2
  1, 0, 0,
])
export const yAxisPositions = new Float32Array([
  // 1
  0, -1, 0,
  // 2
  0, 1, 0,
])
export const zAxisPositions = new Float32Array([
  // 1
  0, 0, -1,
  // 2
  0, 0, 1,
])
