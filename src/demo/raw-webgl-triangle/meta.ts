import type { DemoMeta } from "@/demo";

export const rawWebglTriangleMeta = {
  slug: "raw-webgl-triangle",
  title: "Raw WebGL2 Triangle",
  stage: "Stage 1 / WebGL fundamentals",
  description:
    "A hand-wired WebGL2 triangle with shaders, a vertex buffer, an attribute pointer, and one draw call."
} satisfies DemoMeta;
